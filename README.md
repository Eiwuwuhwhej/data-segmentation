# data-segmentation
A lightweight web application that allows users to upload a CSV file, processes customer data using RFM analysis and K-Means clustering on a backend server, and displays the results on a simple dashboard.
